import React from 'react';
import { GenericModulePage } from '../common/GenericModulePage';

export const StockOffLoadPage: React.FC = () => {
  return (
    <GenericModulePage 
      title="Stock Off Load" 
      subtitle="Manage Stock Offload" 
      entityName="Record" 
      items={[]} 
    />
  );
};

